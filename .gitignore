# Console secrets and personal data. NEVER commit (CLAUDE.md section 3).
sandbox/
*.sed
boot9.bin
essential.exefs
Launcher.dat

# Console photos and development session screenshots
sample/
/*.png

# Visual reference mockup, not production code
prototype/

# Redundant: the exe is already extracted in tools/save3ds/
tools/save3ds.zip

# Runtime state of the real SD guard (per-machine baseline)
tests/.real_sd_hash

# PyInstaller output (the portable exe is generated, not versioned)
build/
dist/

# Caches
__pycache__/
*.pyc
.pytest_cache/
.playwright-mcp/
.venv/
.cache/

# Locally built native helper (Linux/macOS, tools/build_save3ds_macos.sh).
# Compiled binaries never enter the repo; only the official wwylele release exe.
tools/save3ds/save3ds_fuse
